'use client';

import { ChatWorkspace } from '@/components/chat/ChatWorkspace';

/** New conversation — URL: /chat */
export default function ChatPage() {
  return <ChatWorkspace sessionId={null} />;
}
